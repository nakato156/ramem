[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ramem-cli"
version = "1.0.0"
description = "Local-first conversational RAG memory CLI"
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "Apache-2.0" }
dependencies = [
  "huggingface-hub>=0.34,<2",
  "lancedb>=0.25,<1",
  "numpy>=2.0,<3",
  "platformdirs>=4.3,<5",
  "prompt-toolkit>=3.0,<4",
  "pydantic>=2.8,<3",
  "python-dotenv>=1.1,<2",
  "PyYAML>=6.0,<7",
  "rich>=13.9,<15",
  "sentence-transformers>=5,<6",
  "typer>=0.12,<1",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.11,<2",
  "pre-commit>=3.7,<5",
  "pytest>=8.3,<9",
  "pytest-cov>=5,<7",
  "pexpect>=4.9,<5; platform_system != 'Windows'",
  "ruff>=0.6,<1",
  "types-PyYAML>=6.0,<7",
]
training = [
  "accelerate>=1.10,<2",
  "bitsandbytes>=0.47,<1",
  "datasets>=4.0,<5",
  "huggingface-hub>=0.34,<2",
  "peft>=0.17,<1",
  "safetensors>=0.5,<1",
  "torch==2.5.1",
  "transformers>=4.56,<6",
  "trl>=0.23,<1",
]
retrieval = [
  "psutil>=6,<8",
  "torch>=2.6,<2.7",
  "transformers>=4.56,<5",
]
llama = [
  "llama-cpp-python>=0.3.16,<1",
]
transformers = [
  "accelerate>=1.10,<2",
  "bitsandbytes>=0.47,<1; platform_system != 'Darwin'",
  "safetensors>=0.5,<1",
  "torch==2.5.1",
  "transformers>=4.56,<6",
]

[project.scripts]
ramem = "ramem.cli.app:app"
ramem-download = "ramem.training.data:main"
ramem-prepare = "ramem.training.prepare:main"
ramem-train = "ramem.training.train:main"
ramem-evaluate = "ramem.evaluation.generation:main"
ramem-export = "ramem.training.export:main"
ramem-prepare-external = "ramem.evaluation.prepare:main"
ramem-analyze-evaluation = "ramem.evaluation.analysis:main"
ramem-evaluate-retrieval = "ramem.evaluation.retrieval:main"
ramem-release-gates = "ramem.evaluation.release_gates:main"
ramem-adapt-memory-benchmark = "ramem.evaluation.benchmark_adapters:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ramem"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["ramem"]
warn_unreachable = true

[[tool.mypy.overrides]]
module = [
  "ramem.cli.app",
  "ramem.generation.backends",
  "ramem.evaluation.generation",
  "ramem.evaluation.prepare",
  "ramem.generation.transformers",
  "ramem.memory.embeddings",
  "ramem.memory.lance",
  "ramem.training.data",
  "ramem.training.export",
  "ramem.training.prepare",
  "ramem.training.train",
]
# These modules are the runtime boundary for optional ML dependencies whose
# installed packages expose incomplete or unstable type metadata.
disable_error_code = [
  "assignment",
  "attr-defined",
  "import-not-found",
  "import-untyped",
  "misc",
  "no-any-return",
  "no-untyped-call",
  "union-attr",
  "unused-ignore",
]

[tool.uv]
conflicts = [
  [
    { extra = "training" },
    { extra = "retrieval" },
  ],
  [
    { extra = "transformers" },
    { extra = "retrieval" },
  ],
]

[tool.uv.sources]
torch = [
  { index = "pytorch-cu121", extra = "training" },
  { index = "pytorch-cu121", extra = "transformers" },
  { index = "pytorch-cu124", extra = "retrieval" },
]

[[tool.uv.index]]
name = "pytorch-cu121"
url = "https://download.pytorch.org/whl/cu121"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true
